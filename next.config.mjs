/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','links.papareact.com'], // Add the external image domain here
    },
};

export default nextConfig;
